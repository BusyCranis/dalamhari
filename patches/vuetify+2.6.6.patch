diff --git a/node_modules/vuetify/src/components/VAppBar/VAppBar.ts b/node_modules/vuetify/src/components/VAppBar/VAppBar.ts
index 4fdc5f8..7cbfc7c 100644
--- a/node_modules/vuetify/src/components/VAppBar/VAppBar.ts
+++ b/node_modules/vuetify/src/components/VAppBar/VAppBar.ts
@@ -20,6 +20,10 @@ import mixins from '../../util/mixins'
 // Types
 import { VNode } from 'vue'
 
+
+console.log(VToolbar)
+
+
 const baseMixins = mixins(
   VToolbar,
   Scrollable,
@@ -36,6 +40,11 @@ const baseMixins = mixins(
   ])
 )
 
+
+
+console.log(VToolbar)
+
+
 /* @vue/component */
 export default baseMixins.extend({
   name: 'v-app-bar',
@@ -231,6 +240,13 @@ export default baseMixins.extend({
   },
 
   created () {
+
+
+
+    console.log(VToolbar)
+
+
+
     if (this.invertedScroll) this.isActive = false
   },
 
@@ -267,6 +283,15 @@ export default baseMixins.extend({
   },
 
   render (h): VNode {
+
+
+
+    console.log(VToolbar)
+
+
+
+
+
     const render = VToolbar.options.render.call(this, h)
 
     render.data = render.data || {}
diff --git a/node_modules/vuetify/src/components/VMenu/_variables.scss b/node_modules/vuetify/src/components/VMenu/_variables.scss
index bcff98d..f95e620 100644
--- a/node_modules/vuetify/src/components/VMenu/_variables.scss
+++ b/node_modules/vuetify/src/components/VMenu/_variables.scss
@@ -1,4 +1,3 @@
 @import '../../styles/styles.sass';
-
 $menu-content-border-radius: $border-radius-root !default;
-$menu-content-elevation: 8 !default;
+$menu-content-elevation: 0 !default;
\ No newline at end of file
